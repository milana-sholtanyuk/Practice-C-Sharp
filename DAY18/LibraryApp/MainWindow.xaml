<Window x:Class="LibraryApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:LibraryApp.Converters"
        xmlns:vm="clr-namespace:LibraryApp.ViewModels"
        Title="📚 Электронная библиотека" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <conv:StatusToTextConverter x:Key="StatusToTextConverter"/>
        <conv:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Window.DataContext>
        <vm:BookViewModel/>
    </Window.DataContext>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Файл">
                <MenuItem Header="Выход" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="_Книги">
                <MenuItem Header="Загрузить" Command="{Binding LoadCommand}"/>
                <MenuItem Header="Добавить" Command="{Binding AddCommand}"/>
                <MenuItem Header="Редактировать" Command="{Binding EditCommand}"/>
                <MenuItem Header="Удалить" Command="{Binding DeleteCommand}"/>
                <Separator/>
                <MenuItem Header="Выдать книгу" Command="{Binding BorrowCommand}"/>
                <MenuItem Header="Вернуть книгу" Command="{Binding ReturnCommand}"/>
            </MenuItem>
        </Menu>

        <ToolBar DockPanel.Dock="Top">
            <Button Content="🔄 Загрузить" Command="{Binding LoadCommand}"/>
            <Button Content="➕ Добавить" Command="{Binding AddCommand}"/>
            <Button Content="✏ Редактировать" Command="{Binding EditCommand}"/>
            <Button Content="❌ Удалить" Command="{Binding DeleteCommand}"/>
            <Separator/>
            <Button Content="📖 Выдать" Command="{Binding BorrowCommand}"/>
            <Button Content="📕 Вернуть" Command="{Binding ReturnCommand}"/>
        </ToolBar>

        <Border Padding="15" Background="#F5F5F5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="📚 Электронная библиотека" FontSize="28" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#2C3E50"/>

                <Border Grid.Row="1" Background="#E8F4FD" CornerRadius="8" Padding="10" Margin="0,0,0,10"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="⏳ Загрузка..." HorizontalAlignment="Center" FontWeight="Bold"/>
                </Border>

                <TextBox Grid.Row="2" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Height="35" Margin="0,0,0,10" VerticalContentAlignment="Center"
                         FontSize="14" ToolTip="Поиск по названию, автору или жанру"/>

                <Grid Grid.Row="3" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding FilterAuthor, UpdateSourceTrigger=PropertyChanged}" 
                             Margin="0,0,5,0" Height="30" VerticalContentAlignment="Center" ToolTip="Фильтр по автору"/>
                    <TextBox Grid.Column="1" Text="{Binding FilterGenre, UpdateSourceTrigger=PropertyChanged}" 
                             Height="30" VerticalContentAlignment="Center" ToolTip="Фильтр по жанру"/>
                </Grid>

                <DataGrid Grid.Row="4" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                          AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                          RowHeight="50" Margin="0,0,0,10" Background="White" 
                          BorderBrush="#DDD" BorderThickness="1">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="2*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Автор" Binding="{Binding Author}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Жанр" Binding="{Binding Genre}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Год" Binding="{Binding Year}" Width="60">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="Статус" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Padding="6,2" 
                                            Background="{Binding IsAvailable, Converter={StaticResource StatusToColorConverter}}"
                                            HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding IsAvailable, Converter={StaticResource StatusToTextConverter}}"
                                                   Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Grid.Row="5" Text="💡 Выберите книгу и нажмите 'Выдать' или 'Вернуть'" 
                           HorizontalAlignment="Center" Margin="0,15,0,0" Foreground="#888" FontStyle="Italic"/>
            </Grid>
        </Border>
    </DockPanel>
</Window>